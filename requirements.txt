# Core ML Framework
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

# PyTorch Dependencies (Required because we use --no-deps)
diskcache
filelock
fsspec
jinja2
MarkupSafe  # Dependency of jinja2
networkx
sympy
mpmath      # Dependency of sympy

# Reinforcement Learning (Install with --no-deps to avoid metadata conflict)
tensordict==0.5.0
torchrl==0.5.0

# Dependencies often pulled by tensordict/torchrl
cloudpickle
orjson
packaging
numpy==1.24.1

# BricksRL Dependencies
hydra-core==1.3.2
omegaconf
wandb==0.16.1
opencv-python==4.9.0.80
moviepy==1.0.3
tqdm==4.66.1
pynput
pybricksdev
